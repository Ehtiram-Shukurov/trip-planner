<div class="container d-flex justify-content-center align-items-center">
  <div class="card shadow-lg w-100" style="max-width: 400px; border-radius: 10px;">
    <div class="card-body text-center">

      <h1 class="card-title mb-3" contenteditable="true" id="title" {{on "focusout" this.saveTitle}}>
        {{#if this.model.title}}
          {{this.model.title}}
        {{else}}
          Edit Trip Name
        {{/if}}</h1>
      <h5 class="card-subtitle mb-4 text-muted">Destinations</h5>

      <div class="vertical-card-list">
        {{#each this.model.dates as |date|}}
        <div class="carousel-card">
        <LinkTo @route="date.detail"
                @models={{array this.model.trip_id date.id}}
                class="btn btn-light w-100">

          {{date.date}}
        </LinkTo>
        </div>
        {{/each}}
      </div>

      <div class="d-flex float-end mt-4">
        <button class="btn btn-success rounded-circle" type="button" {{on "click" this.saveAndNavigate}}>
          <i class="bi bi-check"></i>
        </button>
      </div>
    </div>
  </div>
</div>

{{outlet}}

