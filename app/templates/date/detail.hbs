{{page-title "Detail"}}

<div class="card shadow p-3 mb-5">
  <h2 class="text-center mb-4">Date: {{this.model.date}}</h2>

  <ul class="list-group">
  {{#each this.model.activities as |activity index|}}
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <div>
      <strong style="color: black;">{{activity.time}}</strong>
      <span class="text d-block">{{activity.location}}</span>
    </div>
    <span class="badge bg-secondary">{{activity.budget}}</span>

    <LinkTo
    @route="editactivity"
    @models={{array this.model.trip_id this.model.date_id index}}
    class="btn btn-outline-secondary btn-sm"
  >
    Edit
  </LinkTo>
    <button
      class="btn btn-outline-danger btn-sm" type="button" {{on "click" (fn this.deleteActivity index)}}>
      Delete
    </button>
  </li>
  {{/each}}
</ul>

  <div class="mt-4 text-center">

    <LinkTo @route="addactivity" @models={{array this.model.trip_id this.model.date_id}}>
      <button class="btn btn-primary w-100" style="max-width: 300px;" type="button">
        Add Activity +
      </button>
    </LinkTo>

  </div>
</div>
<div class="d-flex justify-content-between mt-4">
  <LinkTo @route="date" @model={{this.model.trip_id}}>
    <button class="btn btn-primary rounded-circle" type="button">
    <i class="bi bi-arrow-left"></i>
  </button>
  </LinkTo>
  <LinkTo @route="date" @model={{this.model.trip_id}}>
    <button class="btn btn-primary rounded-circle" type="button">
      <i class="bi bi-check"></i>
    </button>
  </LinkTo>
</div>
{{outlet}}